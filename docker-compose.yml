services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: community_dates
      POSTGRES_USER: community
      POSTGRES_PASSWORD: community
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
